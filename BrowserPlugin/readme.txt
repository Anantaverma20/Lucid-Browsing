InterestLens Chrome extension

How to load:
1) Open Chrome -> chrome://extensions
2) Enable "Developer mode"
3) Click "Load unpacked" and select this folder

What it does:
- Injects a fixed side panel/frame on every page
- Randomly selects links from the current page
- Shows cards with title, image, and description fetched from the link